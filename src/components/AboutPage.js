
import React from 'react';   // If We have only HTML is not necessary to import Component


const AboutPage = () => (

    <div>
        <h1>This is About Page</h1>
    </div>
);

export default AboutPage;
